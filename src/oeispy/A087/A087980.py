# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A087980

from math import prod
from itertools import count
from functools import lru_cache
from sympy import prime, integer_log
from oeis_sequences.OEISsequences import bisection
def A087980(n):
    @lru_cache(maxsize=None)
    def A006939(n): return prod(prime(k)**(n-k+1) for k in range(1,n+1))
    @lru_cache(maxsize=None)
    def g(x, m, j): return sum(g(x//(prime(m)**i), m-1, i) for i in range(j+1,integer_log(x, prime(m))[0]+1)) if m-1 else max(0,x.bit_length()-1-j)
    def f(x):
        c = n-1+x
        for k in count(1):
            if A006939(k)>x:
                break
            c -= g(x,k,0)
        return c
    return bisection(f,n,n) # _Chai Wah Wu_, Mar 23 2026

