# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A397205

from itertools import combinations
def product_distinct(A, B):
    products = [a*b for a in A for b in B]
    return len(products) == len(set(products))
def a(n):
    S = range(1, n+1)
    subsets = [c for r in range(n+1) for c in combinations(S, r)]
    best = 0
    for A in subsets:
        for B in subsets:
            value = len(A)*len(B)
            if value > best and product_distinct(A, B):
                best = value
    return best

