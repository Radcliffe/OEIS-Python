# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A141399

from itertools import islice
from heapq import heappop, heappush
from sympy import integer_nthroot, nextprime
def A141399_gen(): # generator of terms
    h, hset = [(1,(1,))], {1}
    while True:
        m, ps = heappop(h)
        a, b = integer_nthroot(m+1,2)
        if b:
            yield a-1>>1
        for p in ps:
            mp = m*p
            if mp not in hset:
                heappush(h,(mp,ps))
                hset.add(mp)
        q = nextprime(max(ps,default=1))
        mp = m*q
        if mp not in hset:
            heappush(h,(mp,(ps+(q,))))
            hset.add(mp)
A141399_list = list(islice(A141399_gen(),28)) # _Chai Wah Wu_, Apr 05 2026

