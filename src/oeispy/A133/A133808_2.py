# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A133808

from itertools import islice
from heapq import heappop, heappush
from sympy import factorint, nextprime
def A133808_gen(): # generator of terms if the first n terms are desired
    h, hset = [1], {1}
    while True:
        yield (m:=heappop(h))
        ps = factorint(m)
        for p in ps:
            if p == max(ps) or ps[nextprime(p)]>ps[p]:
                mp = m*p
                if mp not in hset:
                    heappush(h,mp)
                    hset.add(mp)
        mp = 2 if m==1 else m*nextprime(a:=max(ps.keys()))**ps[a]
        if mp not in hset:
            heappush(h,mp)
            hset.add(mp)
A133808_list = list(islice(A133808_gen(),40)) # _Chai Wah Wu_, Mar 23 2026

