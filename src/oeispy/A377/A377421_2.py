# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A377421

# alternate program constructing terms directly from primes
from sympy import primerange
def auptobits(maxbits):
    alst = []
    for p in primerange(3, 1<<maxbits):
        b = bin(p)[2:]; br = b[::-1]; t = int(br, 2)
        if br != b: alst.append(t)
        alst.extend(t<<i for i in range(1, maxbits-len(br)+1))
    return sorted(alst)
print(auptobits(8)) # _Michael S. Branicky_, Oct 29 2024

