# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A377093

from itertools import count, islice
def A377093gen(): # generator of terms
    aset, alst, m = {1, 2, 4}, [1, 2, 4], 3
    yield from [1, 2, 4]
    for n in count(3):
        nlst = []
        for k in count(m, m):
            if k not in aset:
                nlst.append(k)
                if len(nlst) == m:
                    break
        yield from nlst
        alst.extend(nlst)
        aset.update(nlst)
        while m in aset: m += 1
print(list(islice(A377093gen(), 70))) # _Michael S. Branicky_, Oct 16 2024

