# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A007628

import itertools; from sympy import isprime
def A007628(n):
  if not hasattr(A:=A007628, 'seq'): A.seq = [13, 31]
  while len(A.seq)<n:
    L = len(str(A.seq[-1])); T = 10**L; B = T*2; C = B+2; new = []
    def do(x,y): isprime(x) and isprime(y) and new.extend((x,y))
    for D in itertools.product((0, 1, 3, 8), repeat = L-1):
      N = t = 1; R = T+1
      for d in D: N = N*10 + d; t*=10; R += d*t
      N = N*10+1; do(N+2, R+B); N<R and (do(N,R), do(N+C,R+C))
    new.sort(); A.seq.extend(new)
  return A.seq[n-1] # _M. F. Hasler_, Jun 30 2026

