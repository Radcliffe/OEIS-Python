# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A385934

from heapq import heappop, heappush
from sympy import factorint, isprime, primefactors, nextprime
def A385934_gen(): # generator of terms
    def is_squarefree(n): return max(factorint(n).values(),default=1)<=1
    h, hset = [1], {1}
    while True:
        m = heappop(h)
        if isprime(m+1):
            yield m+1
        ps = primefactors(m)
        ps.append(nextprime(max(ps,default=1)))
        for p in ps:
            mp = m*p
            if mp not in hset:
                heappush(h,mp)
                hset.add(mp)
A385934_list = list(islice(A385934_gen(),40)) # _Chai Wah Wu_, Mar 21 2026

