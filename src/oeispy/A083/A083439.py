# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A083439

from itertools import islice
from sympy import isprime, nextprime
def agen(): # generator of terms
    an, nextp, seen = 3, 7, set()
    while True:
        yield an
        seen.add(an)
        while nextp%6 != 1 or nextp in seen:
            nextp = nextprime(nextp)
        k, san = nextp, str(an)
        while k%6 != 1 or k in seen or not isprime(int(san+(sk:=str(k)))) or not isprime(int(sk+san)):
            k = nextprime(k)
        an = k
print(list(islice(agen(), 56))) # _Michael S. Branicky_, May 24 2026

