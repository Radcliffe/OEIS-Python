# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A329897

from itertools import count, islice
from heapq import heappop, heappush
from sympy import multiplicity, factorint, prevprime, nextprime
def A329897_gen(): # generator of terms
    h, hset = [1], {1}
    for c in count(1):
        m = heappop(h)
        if multiplicity(3,m)==(~m&m-1).bit_length():
            yield c
        ps = factorint(m)
        for p in ps:
            if p == 2 or ps[prevprime(p)]>ps[p]:
                mp = m*p
                if mp not in hset:
                    heappush(h,mp)
                    hset.add(mp)
        mp = m*nextprime(max(ps.keys(),default=1))
        if mp not in hset:
            heappush(h,mp)
            hset.add(mp)
A329897_list = list(islice(A329897_gen(),69)) # _Chai Wah Wu_, Mar 31 2026

