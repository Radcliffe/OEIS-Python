# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A329894

from itertools import islice
from heapq import heappop, heappush
from sympy import nextprime, isprime, factorint, prevprime
def A329894_gen(): # generator of terms
    h, hset = [1], {1}
    while True:
        m = heappop(h)
        q = nextprime(m)
        if q-m>1 and not isprime(q-m):
            yield m
        ps = factorint(m)
        for p in ps:
            if p == 2 or ps[prevprime(p)]>ps[p]:
                mp = m*p
                if mp not in hset:
                    heappush(h,mp)
                    hset.add(mp)
        mp = m*nextprime(max(ps.keys(),default=1))
        if mp not in hset:
            heappush(h,mp)
            hset.add(mp)
A329894_list = list(islice(A329894_gen(),30)) # _Chai Wah Wu_, Mar 24 2026

