# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A384908

from itertools import count, islice
def agen(): # generator of terms
    L, nextL, mink, cset = [1, 2], 3, 1, set() # cset is set of centers used
    while True:
        c, m = next((k, i) for k in count(mink) if k not in cset and (i:=L.index(k))>0)
        if len(L) < 2*m+1:
            L += list(range(nextL, 2*m+2))
            nextL = 2*m+2
        for i in range(m):
            L[i], L[i+m+1] = L[i+m+1], L[i]
        while mink in cset:
            mink += 1
        yield L[0]
        cset.add(c)
print(list(islice(agen(), 80))) # _Michael S. Branicky_, Jun 12 2025

