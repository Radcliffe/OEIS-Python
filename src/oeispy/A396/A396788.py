# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A396788

from math import isqrt, comb
from sympy import primepi, primerange
from oeis_sequences.OEISsequences import bisection
def A396788(n):
    n10 = 10**n
    def f(x):
        s = isqrt(x)
        a, c, k, l = 0, 0, 1, len(str(s))
        for i in range(1,l):
            k *= 10
            c -= comb((b:=primepi(k-1))-a+1,2)
            a = b
        return c+int(n10+x+sum(max(i,primepi((m:=10**(len(str(p))-1))-1))-primepi(min(x//p,10*m-1)) for i,p in enumerate(primerange(10**(l-1),s+1),a)))
    return bisection(f,n10,n10)

