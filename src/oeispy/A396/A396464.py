# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A396464

from sympy import divisors
def A396464(n):
    terms = [1, 1]
    addflag = False
    while len(terms) < n:
        if addflag:
            terms.append(terms[-1] + terms[-2])
        else:
            le = list(reversed(list(divisors(terms[-1]))[1:-1]))
            divflag = False
            for x in le:
                if x in terms:
                    terms.append(terms[-1] // x)
                    divflag = True
                    break
            if not divflag:
                terms.append(terms[-1] + terms[-2])
        addflag = not addflag
    return terms

