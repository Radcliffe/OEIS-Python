# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A396197

def A396197(num_rows):
    A = [1, 4]
    for _ in range(num_rows + 1): A.append(A[-1] + A[-2])
    tri = [[1]] # T(0,0) = 1
    for n in range(1, num_rows):
        prev = tri[-1]; row = []
        for k in range(n + 1):
            a = b = 0
            if k < n: a = prev[k] * A[n - k] // A[n - 1 - k]
            if k > 0: b = prev[k - 1]
            row.append(a - b)
        tri.append(row)
    return tri
T = A396197(9)
for i, row in enumerate(T):
    r = ", ".join(f"{num:5}" for num in row)
    print(f"[{i}] {r}")

