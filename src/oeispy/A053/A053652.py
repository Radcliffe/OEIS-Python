# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A053652

from itertools import islice
from sympy import isprime, nextprime
from sympy.utilities.iterables import multiset_permutations
def agen(): # generator of terms
    p, q, terms, digits = 2, 3, set(), 1
    while True:
        s = sorted(str(p*q))
        if len(s) > digits:
            yield from sorted(terms)
            terms, digits = set(), len(s)
        for w in multiset_permutations(s):
            if w[0] != '0' and isprime(t:=int("".join(w))):
                terms.add(t)
        p, q = q, nextprime(q)
print(list(islice(agen(), 45))) # _Michael S. Branicky_, Jun 12 2026

