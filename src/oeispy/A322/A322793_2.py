# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A322793

from itertools import islice
from math import prod
from heapq import heappop, heappush
from sympy import nextprime
def A322793_gen(): # generator of terms if the first n terms are desired.
    h, hset = [(4,(2,),2)], {4}
    while True:
        m, ps, e = heappop(h)
        yield m
        for p in ps:
            mp = m*prod(ps)
            if mp not in hset:
                heappush(h,(mp,ps,e+1))
                hset.add(mp)
        q = nextprime(max(ps,default=1))
        mp = m*q**e
        if mp not in hset:
            heappush(h,(mp,(ps+(q,)),e))
            hset.add(mp)
A322793_list = list(islice(A322793_gen(),40)) # _Chai Wah Wu_, Apr 04 2026

