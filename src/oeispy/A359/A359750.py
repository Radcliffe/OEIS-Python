# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A359750

from itertools import count, islice
from functools import lru_cache
from sympy import integer_log
from oeis_sequences.OEISsequences import bisection
def A359750_gen(): # generator of terms
    @lru_cache(maxsize=None)
    def g(x, m, j): return sum(g(x//m**i, m-1, i) for i in range(j,integer_log(x, m)[0]+1)) if m-2 else max(0,x.bit_length()-j)
    def f(x):
        c, f = n-1+x, 1
        for k in count(2):
            f *= k
            if f>x:
                break
            c -= g(x,k,1)
        return c
    a, b = 0, 0
    for n in count(1):
        k = bisection(f,max(b,n),max(b,n))
        if k == b and k != a:
            yield k
        a, b = b, k
A359750_list = list(islice(A359750_gen(),30)) # _Chai Wah Wu_, Mar 29 2026

