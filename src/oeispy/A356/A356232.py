# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A356232

from itertools import count
from functools import lru_cache
from sympy import prime, integer_log
from oeis_sequences.OEISsequences import bisection
def A356232(n):
    @lru_cache(maxsize=None)
    def g(x, m): return sum(g(x//(prime((m<<1)+1)**i), m-1) for i in range(1,integer_log(x, prime((m<<1)+1))[0]+1)) if m else x.bit_length()-1
    def f(x):
        c, p = n-1+x, 1
        for k in count(0):
            p *= prime((k<<1)+1)
            if p>x:
                break
            c -= g(x,k)
        return c
    return bisection(f,n,n) # _Chai Wah Wu_, Mar 21 2026

