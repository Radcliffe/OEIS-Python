# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A052036

def A052036(n):
    sl = len(str(n))
    l, w = divmod(sl,2)
    l10 = 10**l
    if w:
        x = l+1
        t = 10**(x-1)
        t2 = t*10
        for y in range(n//l10,t2):
            k, m = y//10, 0
            while k >= 10:
                k, r = divmod(k,10)
                m = 10*m + r
            z = y*t + 10*m + k
            if z >= n:
                return z-n
    else:
        x = l
        t = 10**(x-1)
        t2 = t*10
        for y in range(n//l10,t2):
            k, m = y, 0
            while k >= 10:
                k, r = divmod(k,10)
                m = 10*m + r
            z = y*t2 + 10*m + k
            if z >= n:
                return z-n # _Chai Wah Wu_, Feb 05 2026

