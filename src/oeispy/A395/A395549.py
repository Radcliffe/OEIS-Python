# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A395549

from fractions import Fraction
from sympy import prime
def A395549(n):
    p = prime(n)
    if p == 2: return None
    h = [Fraction(0)]*p
    for j in range(1, p-1):
        h[j] = Fraction(1, j*(j+1))
    E = [Fraction(0)]*p
    E[0] = Fraction(1)
    for m in range(1, p):
        s = Fraction(0)
        for k in range(1, m+1):
            s += k*h[k]*E[m-k]
        E[m] = s/m
    Ep = E[p-1]
    return (Ep.numerator * pow(Ep.denominator, -1, p)) % p

