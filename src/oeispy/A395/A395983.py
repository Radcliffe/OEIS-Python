# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A395983

from itertools import islice
from collections import defaultdict
from sympy import primerange
def A395983_generator():
    # If the twin prime conjecture is false, this program will get stuck at the last twin prime pair.
    primes = primerange(5,float('inf'))
    while True:
        P = []
        p0 = float('inf')
        I = defaultdict(list)
        for i,p in enumerate(primes):
            g = p-p0
            if g == 2: break
            P.append(p)
            if i: I[g].append(i-1)
            p0 = p
        deleted = [False]*len(P)
        deleted[-1] = True
        for g in range(4,max(I,default=0)+1,2):
            for i in I[g]:
                if not deleted[i] and not deleted[i+1]:
                  deleted[i:i+2] = True,True
        for p,d in zip(P,deleted):
            if not d: yield p
print(list(islice(A395983_generator(),48))) # _Pontus von Brömssen_, May 15 2026

