# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A363455

from itertools import count
from functools import lru_cache
from sympy import prime, integer_log, primorial, factorint
from oeis_sequences.OEISsequences import bisection
def A363455(n):
    @lru_cache(maxsize=None)
    def g(x, m, j): return sum(g(x//(prime(m)**i), m-1, i) for i in range(j,integer_log(x, prime(m))[0]+1)) if m-1 else max(0,x.bit_length()-j)
    def f(x):
        c = n-1+x
        for k in count(1):
            if primorial(k)>x:
                break
            c -= g(x,k,1)
        return c
    return len(set(factorint(bisection(f,n,n)).values())) # _Chai Wah Wu_, Mar 23 2026

