# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A126430

keep = [1, 3]
for aa in range(50):
    flag = False
    temp = set()
    for x in keep:
        for y in keep:
            if x != y:
                temp.add(x+y)
                temp.add(abs(x-y))
            temp.add(x)
            temp.add(y)
    now = sorted(list(temp))
    for x in range(1, len(now)):
        if now[x] - now[x-1] != 1:
            keep.append(keep[-1]+now[x-1]+1)
            flag = True
            break
    if not flag:
        keep.append(keep[-1]+max(now)+1)
print(keep) # _David Consiglio, Jr._, Oct 09 2015

