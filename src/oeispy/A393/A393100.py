# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A393100

from mpmath import iv
def pdf(k):
    return 1/iv.log(k+1,2) - 1/iv.log(k+2,2)
def sample_log_tail_distribution(num_coeffs):
  coeffs, counts = [], [0]
  for _ in range(num_coeffs):
    min_time = iv.inf
    for i, count in enumerate(counts, start=1):
      time = (count+1) / pdf(i)
      if time < min_time:
        min_index, min_time = i, time
    if min_index == len(counts):
      counts.append(0)
    counts[min_index-1] += 1
    coeffs.append(min_index)
  return coeffs
A393100 = sample_log_tail_distribution(120)

