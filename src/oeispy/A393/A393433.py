# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A393433

def ss1(): # generator of square spiral coordinates in 1st quadrant
    yield 0, 0
    delta = [(0, 1), (-1, 0), (1, 0), (0, -1)]
    i, j, L, c = 1, 0, 1, 0
    while True:
        yield i, j
        for s in range(2):
            for k in range(L):
                i, j = i+delta[c][0], j+delta[c][1]
                yield i, j
            c = (c+1)%4
        (i, j) = (i, j+1) if not i else (i+1, j)
        L += 1
def agen(): # generator of terms
    p, an, kloc = set(), 0, (0, 0)
    K = {(2, 1), (1, 2), (-1, 2), (-2, 1), (-2, -1), (-1, -2), (1, -2), (2, -1)}
    while True:
        yield an
        p.add(kloc)
        free = {newloc for i, j in K if (newloc:=(kloc[0]+i, kloc[1]+j)) not in p and min(newloc) >= 0}
        if len(free) == 0: return
        an, kloc = next((k, loc) for k, loc in enumerate(ss1()) if loc in free)
print(list(agen()))

