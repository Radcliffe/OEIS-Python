# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A393389

from itertools import islice
from heapq import heappop, heappush
from sympy import nextprime, isprime, factorint
def A393389_gen(): # generator of terms
    h, hset = [(1,(1,))], {1}
    while True:
        m, ps = heappop(h)
        if not isprime(m+1):
            fs = factorint(m+1)
            if max(fs.values()) == 1 and not any(m % (p-1) for p in fs):
                yield m+1
        for p in ps:
            mp = m*p
            if mp not in hset:
                heappush(h,(mp,ps))
                hset.add(mp)
        q = nextprime(max(ps,default=1))
        mp = m*q
        if mp not in hset:
            heappush(h,(mp,(ps+(q,))))
            hset.add(mp)
A393389_list = list(islice(A393389_gen(),20)) # _Chai Wah Wu_, Apr 02 2026

