# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A393075

from math import gcd
from itertools import count, islice
def is_pow2(k): return k.bit_count() == 1
def agen(): # generator of terms
    yield from [1, 3]
    aset, an, m = {1, 3}, 3, 5
    while True:
        R = int(bin(an)[2:][::-1], 2)
        an = next(k for k in count(m) if k not in aset and gcd(k, R) > 1 and not is_pow2(k))
        yield an
        aset.add(an)
        while m in aset or is_pow2(m): m += 1
print(list(islice(agen(), 67))) # _Michael S. Branicky_, Feb 06 2026

