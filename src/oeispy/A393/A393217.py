# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A393217

from math import gcd
from itertools import count, islice
from sympy import mobius, integer_nthroot, divisors, factorint
from oeis_sequences.OEISsequences import bisection
def A393217_gen():
    w = 1
    def f(x): return int(n-1+x+sum(mobius(k)*(integer_nthroot(x,k)[0]-1) for k in range(2,x.bit_length())))
    for n in count(2):
        w = bisection(f,w,w)
        fs = factorint(w)
        k = gcd(*fs.values())
        s = set(str(w))
        if len(s) < 10:
            for d in divisors(k):
                if d>1:
                    m = integer_nthroot(w,d)[0]
                    if s.isdisjoint(set(str(d**m))):
                        yield w
                        break
A393217_list = list(islice(A393217_gen(),15)) # _Chai Wah Wu_, Feb 13 2026

