# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A393546

from sympy import floor
from functools import cache
from fractions import Fraction
from os.path import commonprefix
def reliable_digits_from_frac(bound1, bound2, prec):
    order = 10**prec
    trunc_bound1 = floor(bound1*order) / order
    trunc_bound2 = floor(bound2*order) / order
    return commonprefix([f'{trunc_bound1:.{prec}f}', f'{trunc_bound2:.{prec}f}'])
@cache
def bronze_fibo(n): return n if n < 2 else 3*bronze_fibo(n-1) + bronze_fibo(n-2)
@cache
def faase(n): return 1 if n < 2 else 3*faase(n-1) + faase(n-2)
def a(n):
    bound1 = Fraction(bronze_fibo(n+1), bronze_fibo(n))
    bound2 = Fraction(faase(n+1), faase(n))
    rel_digits = reliable_digits_from_frac(bound1, bound2, int(1.04*n))
    return len(rel_digits) - 1 if rel_digits else 0

