# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A393747

from functools import lru_cache
@lru_cache(None)
def count_paths_with_k_peaks(x_rem, y_rem, z_rem, x_used, y_used, z_used, last_char, k_needed):
    if x_rem == 0 and y_rem == 0 and z_rem == 0:
        return 1 if k_needed == 0 else 0
    if not (x_used >= y_used >= z_used):
        return 0
    if k_needed < 0:
        return 0
    total = 0
    if x_rem > 0:
        total += count_paths_with_k_peaks(x_rem - 1, y_rem, z_rem,
                                  x_used + 1, y_used, z_used,
                                  0, k_needed)
    if y_rem > 0 and x_used >= y_used + 1:
        total += count_paths_with_k_peaks(x_rem, y_rem - 1, z_rem,
                                  x_used, y_used + 1, z_used,
                                  1, k_needed)
    if z_rem > 0 and y_used >= z_used + 1:
        used = 1 if last_char == 0 else 0
        total += count_paths_with_k_peaks(x_rem, y_rem, z_rem - 1, x_used, y_used, z_used + 1, 2, k_needed - used)
    return total
print(count_paths_with_k_peaks(0, 0, 0, 0, 0, 0, -1, 0))
for n in range(1, 10):
    result = ""
    for k in range(0, n):
        result = result + str(count_paths_with_k_peaks(n, n, n, 0, 0, 0, -1, k)) +","
    print(result)

