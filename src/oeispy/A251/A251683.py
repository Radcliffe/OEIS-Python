# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A251683

import sympy as sp
x = sp.Symbol('x')
b_cache : dict[int, sp.Poly] = {}
def b(n: int) -> sp.Poly:
    if n in b_cache: return b_cache[n]
    divs = [d for d in sp.divisors(n) if d not in (1, n)]
    expr = x * (1 + sum(b(n // d) for d in divs))
    b_cache[n] = sp.expand(expr)
    return b_cache[n]
def A251683_row(n: int) -> list[int]:
    poly = sp.Poly(b(n) / x, x)
    return poly.all_coeffs()[::-1]
print(sp.flatten([A251683_row(n) for n in range(1, 41)]))  # _Peter Luschny_, Nov 06 2025

