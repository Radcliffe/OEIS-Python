# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A030630

from math import isqrt
from sympy import integer_nthroot, primerange, primepi
from oeis_sequences.OEISsequences import bisection
def A030630(n):
    def f(x):
        x4 = integer_nthroot(x,4)[0]+1
        x5 = integer_nthroot(x,5)[0]
        c = n+x
        c += -sum(primepi(x//(k*k*m))-b for a,k in enumerate(primerange(x4),1) for b,m in enumerate(primerange(k+1,isqrt(x//(k*k))+1),a+1))
        c += -sum(primepi(x//(k*m*m))-b for a,k in enumerate(primerange(x4),1) for b,m in enumerate(primerange(k+1,integer_nthroot(x//k,3)[0]+1),a+1))
        c += -sum(primepi(isqrt(x//(k*m)))-b for a,k in enumerate(primerange(x4),1) for b,m in enumerate(primerange(k+1,integer_nthroot(x//k,3)[0]+1),a+1))
        c += -sum(primepi(x//p**5) for p in primerange(x5+1))+primepi(integer_nthroot(x,6)[0])
        c += -sum(primepi(isqrt(x//p**3)) for p in primerange(integer_nthroot(x,3)[0]+1))+primepi(x5)
        c += -primepi(integer_nthroot(x,11)[0])
        return int(c)
    return bisection(f,n,n) # _Chai Wah Wu_, May 01 2026

