# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A308472

def dprod(n):
    x = str(n)
    start = 1
    for q in x:
        start *= int(q)
    return start
def dsum(n):
    x = str(n)
    start = 0
    for q in x:
        start += int(q)
    return start
seq_1 = [n for n in range(1,10000) if dprod(n) != 0 and n % (dsum(dprod(n))) == 0]
print(seq_1)

