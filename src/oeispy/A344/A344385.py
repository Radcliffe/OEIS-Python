# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A344385

from heapq import heappop, heappush
from sympy import isprime, factorint, prevprime, nextprime
def A344385_gen(): # generator of terms
    h, hset = [1], {1}
    while True:
        m = heappop(h)
        if isprime(m-1) or isprime(m+1):
            yield m
        ps = factorint(m)
        for p in ps:
            if p == 2 or ps[prevprime(p)]>ps[p]:
                mp = m*p
                if mp not in hset:
                    heappush(h,mp)
                    hset.add(mp)
        mp = m*nextprime(max(ps.keys(),default=1))
        if mp not in hset:
            heappush(h,mp)
            hset.add(mp)
A344385_list = list(islice(A344385_gen(),40)) # _Chai Wah Wu_, Mar 23 2026

