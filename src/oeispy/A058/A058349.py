# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A058349

def A058349(n: int) -> int:
    """ Also defined for n = 0 and returns 0 in that case. """
    ret, fact = 0, 1
    s = -1 if (n - 1) % 2 else 1
    for i in range(n):
        S, c = 0, 1
        b = i + n - 1
        for k in range(2*i + 1):
            d = k - i
            if d == 0: p = 0 if b > 0 else 1
            else: p = d ** b
            S += c * p * (s * (-1 if d % 2 else 1))
            if k < 2*i: c = c * (2*i - k) // (k + 1)
        fact = fact * i if i > 0 else 1
        ret += S // fact
    return ret
print([A058349(n) for n in range(1, 20)])  # _Peter Luschny_, Mar 29 2026

