# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A137491

from math import isqrt
from sympy import integer_nthroot, primerange, primepi
from oeis_sequences.OEISsequences import bisection
def A137491(n):
    def f(x):
        x8 = integer_nthroot(x,8)[0]+1
        c = n+x
        c += -sum(primepi(x//(k**6*m))-b for a,k in enumerate(primerange(x8),1) for b,m in enumerate(primerange(k+1,isqrt(x//(k**6))+1),a+1))
        c += -sum(primepi(x//(k*m**6))-b for a,k in enumerate(primerange(x8),1) for b,m in enumerate(primerange(k+1,integer_nthroot(x//k,7)[0]+1),a+1))
        c += -sum(primepi(integer_nthroot(x//(k*m),6)[0])-b for a,k in enumerate(primerange(x8),1) for b,m in enumerate(primerange(k+1,integer_nthroot(x//k,7)[0]+1),a+1))
        c += -sum(primepi(integer_nthroot(x//p**6,3)[0]) for p in primerange(integer_nthroot(x,6)[0]+1))+primepi(integer_nthroot(x,9)[0])
        c += -sum(primepi(x//p**13) for p in primerange(integer_nthroot(x,13)[0]+1))+primepi(integer_nthroot(x,14)[0])
        c += -primepi(integer_nthroot(x,27)[0])
        return int(c)
    return bisection(f,n,n) # _Chai Wah Wu_, May 01 2026

