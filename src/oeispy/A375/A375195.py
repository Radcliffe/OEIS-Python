# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A375195

from itertools import islice
from heapq import heappop, heappush
from sympy import divisor_count, factorint, prevprime, nextprime
def A375195_gen(): # generator of terms
    d, h, hset = 0, [1], {1}
    for c in count(0):
        m = heappop(h)
        e = divisor_count(m)
        if d == e:
            yield c
        ps = factorint(m)
        for p in ps:
            if p == 2 or ps[prevprime(p)]>ps[p]:
                mp = m*p
                if mp not in hset:
                    heappush(h,mp)
                    hset.add(mp)
        mp = m*nextprime(max(ps.keys(),default=1))
        if mp not in hset:
            heappush(h,mp)
            hset.add(mp)
        d = e
A375195_list = list(islice(A375195_gen(),30)) # _Chai Wah Wu_, Mar 24 2026

