# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A375552

from itertools import count, islice
from sympy import isprime, nextprime
def A375552_gen(): # generator of terms
    p, a = 2, set()
    yield from (2,5)
    while True:
        q, m = 2, 10
        for l in count(1):
            while q<m:
                if isprime(m*(p+q)+q):
                    if q not in a:
                        yield q
                        a.add(q)
                    break
                q = nextprime(q)
            else:
                m *= 10
                continue
            break
        p = nextprime(p)
A375552_list = print(list(islice(A375552_gen(),61))) # _Chai Wah Wu_, Sep 18 2024

