# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A392511

from itertools import islice
def a_gen():
    L = [()]
    yield 1
    while True:
        L2 = set()
        for p in L:
            for i, l in enumerate(p):
                if i == 0 or len(l) < len(p[i - 1]):
                    p2 = list(p)
                    l2 = list(l)
                    l2.append(1)
                    p2[i] = tuple(l2)
                    L2.add(tuple(p2))
                if i < len(p) - 1:
                    for j in range(min(len(l) - 1, len(p[i + 1]))):
                        if (l[j] == l[j + 1] == p[i + 1][j]
                            and (i == 0 or l[j] < p[i - 1][j])
                            and (j == 0 or l[j] < l[j-1])):
                            p2 = list(p)
                            l2 = list(l)
                            l2[j] += 1
                            p2[i] = tuple(l2)
                            L2.add(tuple(p2))
            p2 = list(p)
            p2.append((1,))
            L2.add(tuple(p2))
        L = L2
        yield len(L)
print(list(islice(a_gen(), 21)))

