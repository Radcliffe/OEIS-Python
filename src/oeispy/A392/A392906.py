# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A392906

import numpy as np
tri = [[0], [1, 0]]
for i in range(2, 100):
    N = i + 1
    row = []
    for j in range(N):
        ray = 0
        for prev in tri:
            L = len(prev)
            if L == 1:
                ray += prev[0]
                continue
            x = j * (L - 1) / (N - 1)
            left = int(np.floor(x))
            right = int(np.ceil(x))
            ray += prev[left]
            if right != left:
                ray += prev[right]
        row.append(ray % 2)
    tri.append(row)
print(tri)
seq=[]
for r in tri:
    sum=0
    for t in range(len(r)):
        sum+=r[t]
    seq.append(sum)
print(seq)

