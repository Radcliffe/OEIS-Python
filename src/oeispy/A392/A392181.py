# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A392181

from itertools import count, islice
def ss1(): # generator of square spiral coordinates in 1st quadrant
    yield 0, 0
    delta = [(0, 1), (-1, 0), (1, 0), (0, -1)]
    i, j, L, c = 1, 0, 1, 0
    while True:
        yield i, j
        for s in range(2):
            for k in range(L):
                i, j = i+delta[c][0], j+delta[c][1]
                yield i, j
            c = (c+1)%4
        (i, j) = (i, j+1) if not i else (i+1, j)
        L += 1
def agen(): # generator of terms
    p, K = set(), {(2, 1), (1, 2), (-1, 2), (-2, 1), (-2, -1), (-1, -2), (1, -2), (2, -1)}
    for k, loc in enumerate(ss1()):
        if all((loc[0]+i, loc[1]+j) not in p for i, j in K):
            p.add(loc)
            yield k
print(list(islice(agen(), 62)))

