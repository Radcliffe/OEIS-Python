# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A265812

from sympy import prevprime, nextprime, pi
def POBA(x):
    q = 2; best = 99
    while p := x*q:
        dL = p - (pL := prevprime(p) if p>2 else 0)
        dR = (pR := nextprime(p)) - p
        if dL < dR and dL < best*q:
            yield (pL,q); best = dL/q
        elif dR < dL and dR < best*q:
            yield (pR,q); best = dR/q
        elif dR == dL and dR < best*q: # can't happen for irrational x
            yield (pR,q); yield (pL,q); best = dR/q
        q = nextprime(q)
A265812_first = lambda N: [p for (p,q),_ in zip(POBA(pi),range(N))] # _M. F. Hasler_, May 18 2026

