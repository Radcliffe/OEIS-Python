# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A391539

def no_sirm(mm):
   """
   returns 0 if every integer is a sum of
    progressive powers of 5^t multiplied by coefficients in {-m,-1,0,1,m};
    otherwise returns the smallest positive integer not fulfilling the above.
   """
   assert isinstance(mm,int)
   mm = abs(mm)
   if mm<2 or mm%5 in (0,1,4): return 2
   untested_lst = list(range(2,mm//4+1))
   tested_lst = [0,1]
   while untested_lst:
      testing = rr = untested_lst[0]
      rr_lst = []
      while rr not in tested_lst:
         rr_lst.append(rr)
         untested_lst.remove(rr)
         if   rr%5==(  0)%5:  rr -= (  0)
         elif rr%5==( +1)%5:  rr -= ( +1)
         elif rr%5==( -1)%5:  rr -= ( -1)
         elif rr%5==(+mm)%5:  rr -= (+mm)
         elif rr%5==(-mm)%5:  rr -= (-mm)
         rr = abs(rr//5)
         if rr in rr_lst: return testing
      tested_lst.extend(rr_lst)
   return 0

