# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A326107

from itertools import count, islice
def cond(an, k):
    return 1 == sum(1 for d in map(int, str(k)) if d and an%d == 0)
def agen(): # generator of terms
    an, m, aset = 1, 2, set()
    while True:
        yield an
        aset.add(an)
        digs = set(map(int, str(an)))
        an = next(k for k in count(m) if k not in aset and cond(an, k))
        while m in aset: m += 1
print(list(islice(agen(), 77))) # _Michael S. Branicky_, May 04 2026

