# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A383781

from gmpy2 import is_prime, mpz
from itertools import count, islice
def agen():
    olst, elst = [2, 3, 5, 7], [11, 19, 29, 31, 41, 59, 61, 71, 79, 89]
    for n in count(1):
        yield from sorted(olst + elst)
        olst2, elst2 = [], []
        for o in olst:
            o, base = o, 10**(2*n-1)
            for i in range(10*base, 100*base, base):
                t = i + o
                t2 = int(str(t)[1:])
                if is_prime(t) and not is_prime(t2):
                    olst2.append(t)
        for e in elst:
            e, base = e, 10**(2*n)
            for i in range(10*base, 100*base, base):
                t = i + e
                t2 = int(str(t)[1:])
                if is_prime(t) and not is_prime(t2):
                    elst2.append(t)
        olst, elst = olst2, elst2
print(list(islice(agen(), 68))) # _Michael S. Branicky_, May 11 2025

