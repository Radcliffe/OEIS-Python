# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A386761

from sympy import factorint
from itertools import count, product, islice
def bgen(): # generator of A031955
    for d in count(2):
        yield from sorted(int("".join(t)) for f in "123456789" for r in "0123456789" if f!=r for s in product(f+r, repeat=d-1) if len(set(t:=(f, )+s)) == 2)
def agen(): # generator of terms
    n, adict = 1, dict()
    for t in bgen():
        v = len(factorint(t))
        if v not in adict:
            adict[v] = t
            while n in adict: yield adict[n]; n += 1
print(list(islice(agen(), 9))) # _Michael S. Branicky_, Oct 11 2025

