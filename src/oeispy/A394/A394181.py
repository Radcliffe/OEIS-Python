# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A394181

S = [2]
T = [2]
a = [2]
def A394181(n):
    while len(a) < n:
        add_flag = False
        for N in sorted(list(set(S))):
            for k in range(1,len(a)+1):
                if k*N > T[-1] and abs(T[-1] - k*N) < abs(T[-1]-(k-1)*N):
                    S.append(N)
                    a.append(N)
                    T.append(k*N)
                    add_flag = True
                if add_flag:
                    break
            if add_flag:
                break
        if add_flag == False:
            for N in [q for q in range(1,100) if q not in S]:
                for k in range(1,len(a)+1):
                    if k*N > T[-1] and abs(T[-1] - k*N) < abs(T[-1]-(k-1)*N):
                        add_flag = True
                        S.append(N)
                        a.append(N)
                        T.append(k*N)
                        break
                if add_flag:
                    break
    return(a)
# _David Consiglio, Jr._, May 22 2026

