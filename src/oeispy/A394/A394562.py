# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A394562

from math import factorial
from fractions import Fraction
def hermite(k):
    return sum(Fraction(1, (factorial(i)*factorial(k-2*i))) for i in range(1+k//2))
def sample_hermite_distribution(num_coeffs):
  coeffs, counts = [], [0,0,0]
  for _ in range(num_coeffs):
    min_time = float('infinity')
    for i, count in enumerate(counts):
      time = (count+1) / hermite(i)
      if time < min_time:
        min_index, min_time = i, time
    if min_index == len(counts)-1:
      counts.append(0)
    counts[min_index] += 1
    coeffs.append(min_index)
  return coeffs
A394562 = sample_hermite_distribution(120)

