# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A394854

from itertools import islice
from functools import lru_cache
@lru_cache
def fibonacci(k):
    return fibonacci(k-1) + fibonacci(k-2) if k > 1 else k
def sample_fibonacci_distribution():
    n, num_reached = 0, 1
    while n := n+1:
        yield 1
        for num in range(2, num_reached+2):
            if n % fibonacci(num+1) == 0:
                yield num
                num_reached += num == num_reached+1
A394854 = list(islice(sample_fibonacci_distribution(), 120))

