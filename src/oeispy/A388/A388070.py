# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A388070

from math import gcd
from functools import reduce
def a(n):
    def comps(n):
        if n == 0:
            yield []
            return
        for i in range(1, n + 1):
            for c in comps(n - i):
                yield [i] + c
    count = 0
    for c in comps(n):
        if not c:
            continue
        s = sorted(c)
        k = len(s)
        med = s[k // 2] if k % 2 else s[k // 2 - 1]
        if reduce(gcd, c) == med:
            count += 1
    return count
print([a(n) for n in range(1, 26)])

