# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A387946

import heapq
from itertools import islice
from sympy import isprime, sieve
def agen(): # generator of terms
    pp, ss, nextcount, alst, oldv = 6, 5, 3, [], -1
    h = [(pp-ss, pp, ss, 1, 2)]
    while True:
        (v, p, s, i, j) = heapq.heappop(h)
        if v > oldv:
            yield v
        oldv = v
        if v >= pp-ss:
            pp *= sieve[nextcount]
            ss += sieve[nextcount]
            heapq.heappush(h, (pp-ss, pp, ss, 1, nextcount))
            nextcount += 1
        p //= sieve[i]; s -= sieve[i]; i += 1; j += 1; p *= sieve[j]; s += sieve[j]
        heapq.heappush(h, (p-s, p, s, i, j))
    return alst
print(list(islice(agen(), 51))) # _Michael S. Branicky_, Nov 30 2025

