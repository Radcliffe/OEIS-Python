# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A387256

from math import isqrt, gcd
from sympy import primepi, integer_nthroot, factorint
from oeis_sequences.OEISsequences import bisection, squarefreepi
def A387256(n):
    def f(x):
        c, l = n+x, 0
        j = z = isqrt(x)
        c += primepi(z)+squarefreepi(integer_nthroot(z,3)[0])-squarefreepi(integer_nthroot(x,3)[0])
        while j>1:
            k2 = integer_nthroot(x//j**2,3)[0]+1
            w = squarefreepi(k2-1)
            c -= j*(w-l)
            l, j = w, isqrt(x//k2**3)
        c += l-sum(isqrt(z//k**3) for k in range(1, integer_nthroot(z, 3)[0]+1) if all(d<=1 for d in factorint(k).values()))
        j, l = isqrt(z), 0
        while j>1:
            k2 = integer_nthroot(z//j**2,3)[0]+1
            w = squarefreepi(k2-1)
            c += j*(w-l)
            l, j = w, isqrt(z//k2**3)
        c += -l
        for w in range(1,integer_nthroot(x,5)[0]+1):
            if all(d<=1 for d in factorint(w).values()):
                for y in range(1,integer_nthroot(z:=x//w**5,4)[0]+1):
                    if gcd(w,y)==1 and all(d<=1 for d in factorint(y).values()):
                        c += integer_nthroot(z//y**4,3)[0]
        return c
    return bisection(f,n,n) # _Chai Wah Wu_, Dec 04 2025

