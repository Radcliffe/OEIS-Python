# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A376027

from itertools import count
from gmpy2 import is_prime, mpz, digits
def f(k):
    i, s = 0, "1"
    while True:
        ski = digits(k**(i+1))
        if not is_prime(mpz(s+ski)):
            break
        i, s = i+1, s+ski
    return i
def afind():
    adict = dict()
    for m in count(1, 2): # or count(3, 6) for a(2) on per comment
        v = f(m)
        if v > 0 and v not in adict:
            adict[v] = m
            print("FOUND", m, v)
afind() # _Michael S. Branicky_, Sep 08 2024

