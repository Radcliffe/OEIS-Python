# This file is auto-generated from OEIS data.
# Please do not edit this file directly.
# OEIS sequence: A285508

from sympy import primefactors, primeomega
def omega(n): return len(primefactors(n))
def bigomega(n): return primeomega(n)
print([n for n in range(1, 501) if omega(n)<3 and bigomega(n) == 3]) # _Indranil Ghosh_, Apr 20 2017 and _Kalle Siukola_, Oct 25 2023
from math import isqrt
from sympy import primepi, primerange, integer_nthroot
def A285508(n):
    def bisection(f,kmin=0,kmax=1):
        while f(kmax) > kmax: kmax <<= 1
        while kmax-kmin > 1:
            kmid = kmax+kmin>>1
            if f(kmid) <= kmid:
                kmax = kmid
            else:
                kmin = kmid
        return kmax
    def f(x): return int(n+x-sum(primepi(x//(k**2))-(a<<1)+primepi(isqrt(x//k))-1 for a,k in enumerate(primerange(integer_nthroot(x,3)[0]+1))))
    return bisection(f,n,n) # _Chai Wah Wu_, Oct 20 2024

